<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tamtam.tamtam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GestionnaireClient" />
        <activity android:name=".GestionnaireProduits" />
        <activity android:name=".GestionnaireRecette" />
        <activity android:name=".GestionnaireEvenement" />
        <activity android:name=".GestionnaireNotification" />
        <activity android:name=".GestionnaireRapport" />
        <activity android:name=".GestionnaireSauvegarde" />
    </application>

</manifest>